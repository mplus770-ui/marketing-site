---
permalink: /robots.txt
eleventyExcludeFromCollections: true
---
{%- set published = locales | onHost("international") | publishable(domains) -%}
{%- if build.indexable and published | length -%}
User-agent: *
Allow: /
Disallow: /concept-gate2/
Disallow: /review-gate3/
Disallow: /hero-motion-review/
Disallow: /logo-review/

# AI assistants are allowed on purpose: being citable is the point of AEO/GEO.
User-agent: GPTBot
Allow: /
User-agent: ClaudeBot
Allow: /
User-agent: PerplexityBot
Allow: /
User-agent: Google-Extended
Allow: /

{# This file is served on the INTERNATIONAL host only. It advertises the
   international sitemap and nothing else — a host must never advertise the
   other host's sitemap. www.zohar-ai.co.il is rewritten to /robots-he.txt. #}
{%- if domains.international %}
Sitemap: {{ domains.international }}/sitemap.xml
{%- endif %}
{%- else -%}
# Not a production build, or no canonical origin is configured.
User-agent: *
Disallow: /
{%- endif -%}
