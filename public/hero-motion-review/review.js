const root=document.documentElement;const buttons=[...document.querySelectorAll('[data-set-lang]')];buttons.forEach(button=>button.addEventListener('click',()=>{const lang=button.dataset.setLang;root.lang=lang;root.dir=lang==='he'?'rtl':'ltr';buttons.forEach(item=>item.setAttribute('aria-pressed',String(item===button)))}));
    const video=document.querySelector('video');const reduced=matchMedia('(prefers-reduced-motion: reduce)').matches;const saveData=navigator.connection?.saveData;if(reduced||saveData){video.removeAttribute('autoplay');video.pause();video.style.display='none'}
    const observer=new IntersectionObserver(([entry])=>{if(reduced||saveData)return;entry.isIntersecting?video.play().catch(()=>{}):video.pause()},{threshold:.15});observer.observe(video);document.addEventListener('visibilitychange',()=>{if(document.hidden)video.pause();else if(!reduced&&!saveData)video.play().catch(()=>{})});
